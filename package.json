{
  "name": "@edge-js/keygrip",
  "version": "1.1.1",
  "description": "Key signing and verification for rotated credentials",
  "license": "MIT",
  "repository": "edgejs/keygrip",
  "dependencies": {
    "tsscmp": "1.0.6"
  },
  "devDependencies": {
    "eslint": "7.16.0",
    "mocha": "8.2.1",
    "nyc": "15.1.0"
  },
  "files": [
    "HISTORY.md",
    "LICENSE",
    "README.md",
    "tsscmp.js",
    "index.js"
  ],
  "engines": {
    "node": ">= 0.6"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "mocha --reporter spec test/",
    "test-cov": "nyc --reporter=html --reporter=text npm test",
    "test-travis": "nyc --reporter=text npm test"
  }
}
